#include <stdio.h>

int main()
{
    int n, a, b, c,d;
    scanf("%d %d %d %d %d", &n,&a, &b, &c, &d);
    int s[n], ans[n];
    for (int i = 1; i <= a; i++)
        s[i] = i;








    for (int j = 0; j < n; j++)
        printf("%d", s[j]);

    return 0;
}
